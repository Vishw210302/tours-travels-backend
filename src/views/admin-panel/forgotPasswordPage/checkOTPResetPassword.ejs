<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Verify OTP</title>
    <style>
      :root {
        --primary-bg: #121212;
        --secondary-bg: #1e1e1e;
        --text-primary: #e0e0e0;
        --text-secondary: #a0a0a0;
        --accent-color: #4a90e2;
        --hover-color: #6ab0ff;
        --border-color: #333333;
        --error-color: #ff6b6b;
        --success-color: #4ecb71;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Inter", sans-serif;
      }

      body {
        background: linear-gradient(135deg, var(--primary-bg) 0%, #0f0f0f 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
      }

      .container {
        background: var(--secondary-bg);
        border-radius: 20px;
        box-shadow: 0 15px 35px rgba(0, 0, 0, 0.4);
        width: 100%;
        max-width: 450px;
        padding: 40px;
        border: 1px solid var(--border-color);
        transition: all 0.5s ease;
      }

      .welcome-text {
        color: var(--text-primary);
        font-size: 28px;
        margin-bottom: 15px;
        font-weight: 700;
        text-align: center;
      }

      .subtitle {
        color: var(--text-secondary);
        font-size: 16px;
        margin-bottom: 30px;
        text-align: center;
        line-height: 1.6;
      }

      .input-field {
        width: 100%;
        padding: 15px;
        margin: 10px 0;
        background: var(--primary-bg);
        border: 1px solid var(--border-color);
        border-radius: 10px;
        color: var(--text-primary);
        font-size: 16px;
        transition: all 0.3s ease;
      }

      .input-field:focus {
        outline: none;
        border-color: var(--accent-color);
        box-shadow: 0 0 0 3px rgba(74, 144, 226, 0.2);
      }

      .submit-button {
        width: 100%;
        padding: 15px;
        background: var(--accent-color);
        color: white;
        border: none;
        border-radius: 10px;
        font-weight: 600;
        letter-spacing: 0.5px;
        cursor: pointer;
        transition: all 0.3s ease;
        margin-top: 20px;
      }

      .submit-button:hover {
        background: var(--hover-color);
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(74, 144, 226, 0.3);
      }

      .label {
        color: var(--text-primary);
        margin-bottom: 5px;
        display: block;
      }

      .error-message {
        color: var(--error-color);
        text-align: center;
        margin-top: 15px;
        font-size: 14px;
      }

      .success-message {
        color: var(--success-color);
        text-align: center;
        margin-top: 15px;
        font-size: 14px;
      }

      .hidden {
        display: none;
      }

      .email-display {
        color: var(--text-secondary);
        text-align: center;
        margin-bottom: 20px;
        font-size: 16px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div id="otpVerificationForm">
        <div class="welcome-text">Verify OTP</div>
        <div class="subtitle">An OTP has been sent to your email</div>

        <div id="emailDisplay" class="email-display"></div>

        <form id="otpForm" action="/verify-otp-password" method="POST">
          <input type="hidden" id="email" name="email" required />

          <label for="otp" class="label">Enter OTP</label>

          <input
            type="text"
            id="otp"
            name="otp"
            class="input-field"
            placeholder="Enter 6-digit OTP"
            required
            maxlength="6"
            pattern="\d{6}"
          />
          <input type="hidden" value="<%- userData._id -%>" name="userId" />
          <div id="otpErrorMessage" class="error-message hidden">
            Incorrect OTP. Please try again.
          </div>
          <button type="submit" class="submit-button">Verify OTP</button>
        </form>
      </div>
    </div>
  </body>
</html>
