<script src="/vendors/js/vendor.bundle.base.js"></script>
<script src="/vendors/chart.js/Chart.min.js"></script>
<script src="/vendors/progressbar.js/progressbar.min.js"></script>
<script src="/vendors/jvectormap/jquery-jvectormap.min.js"></script>
<script src="/vendors/jvectormap/jquery-jvectormap-world-mill-en.js"></script>
<script src="/vendors/owl-carousel-2/owl.carousel.min.js"></script>
<script src="/js/off-canvas.js"></script>
<script src="/js/hoverable-collapse.js"></script>
<script src="/js/misc.js"></script>
<script src="/js/settings.js"></script>
<script src="/js/todolist.js"></script>
<script src="/vendors/select2/select2.min.js" defer></script>
<script src="/js/dashboard.js"></script>
<script src="vendors/typeahead.js/typeahead.bundle.min.js"></script>
<script src="/js/file-upload.js"></script>
<script src="/js/typeahead.js"></script>
<script src="/js/select2.js" defer></script>

<script>
  function allTestimonialListingToggleStatus(checkbox) {
    const testimonialId = checkbox.getAttribute("data-id");
    const newStatus = checkbox.checked ? "Active" : "Inactive";

    fetch(`/admin/testimonial/status/${testimonialId}`, {
      method: "PUT",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ status: newStatus }),
    })
      .then((response) => response.json())
      .then((data) => {
        if (data.message) {
          alert(data.message);
        } else {
          alert("Something went wrong!");
        }
      })
      .catch((error) => {
        console.error("error", error);
        alert("Failed to update status");
      });
  }
</script>

<script>
  function allTestimonialHotelListingToggleStatus(checkbox) {
    const testimonialId = checkbox.getAttribute("data-id");
    const newStatus = checkbox.checked ? "Active" : "Inactive";

    fetch(`/admin/testimonial-hotel/status/${testimonialId}`, {
      method: "PUT",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ status: newStatus }),
    })
      .then((response) => response.json())
      .then((data) => {
        if (data.message) {
          alert(data.message);
        } else {
          alert("Something went wrong in hotel Testimonial");
        }
      })
      .catch((error) => {
        console.error("error", error);
        alert("Failed to update status");
      });
  }
</script>

<script>
  function promoCodeToggleStatus(checkbox) {
    const promoCodeId = checkbox.getAttribute("data-id");
    const status = checkbox.checked ? "Active" : "Inactive";

    fetch(`/admin/promoCodes/status/${promoCodeId}`, {
      method: "PUT",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ status }),
    })
      .then((response) => response.json())
      .then((data) => {
        if (data.message) {
          alert(data.message);
        }
      })
      .catch((error) => {
        console.error("error", error);
        alert("Failed to update status.");
      });
  }
</script>

<script>
  function dicountCouponToggleStatus(checkbox) {
    const discountCodesId = checkbox.getAttribute("data-id");
    const status = checkbox.checked ? "Active" : "Inactive";

    fetch(`/admin/discountCodes/status/${discountCodesId}`, {
      method: "PUT",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ status }),
    })
      .then((response) => response.json())
      .then((data) => {
        if (data.message) {
          alert(data.message);
        }
      })
      .catch((error) => {
        console.error("error", error);
        alert("Failed to update status.");
      });
  }
</script>

<script>
  function packageToggleStatus(checkbox) {
    const packageThemeId = checkbox.getAttribute("data-id");
    const status = checkbox.checked ? "Active" : "Inactive";

    fetch(`/admin/packageTheme/status/${packageThemeId}`, {
      method: "PUT",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ status }),
    })
      .then((response) => response.json())
      .then((data) => {
        if (data.message) {
          alert(data.message);
        }
      })
      .catch((error) => {
        console.error("error", error);
        alert("Failed to update status.");
      });
  }
</script>
